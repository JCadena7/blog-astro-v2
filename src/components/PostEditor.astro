---
import MdxEditorWrapper from './MdxEditorWrapper.jsx';
---

<div class="post-editor">
  <div class="editor-header">
    <h2>Contenido</h2>
    <p class="editor-info">Utiliza las herramientas de la barra superior para formatear tu contenido.</p>
  </div>

  <div class="editor-container">
    <MdxEditorWrapper 
      client:load
      id="post-editor"
      initialContent=""
    />
  </div>
</div>

<style>
.post-editor {
  background: var(--surface-card);
  border-radius: 0.5rem;
  padding: 1.5rem;
  margin-bottom: 1.5rem;
}

.editor-header {
  margin-bottom: 1rem;
}

.editor-header h2 {
  font-size: 1.25rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
  color: var(--text-color);
}

.editor-info {
  font-size: 0.875rem;
  color: var(--text-muted);
}

.editor-container {
  border: 1px solid var(--border-color);
  border-radius: 0.375rem;
  overflow: hidden;
  min-height: 400px;
}
</style> 